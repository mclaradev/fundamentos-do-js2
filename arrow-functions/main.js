const arrowSum = (a, b) => {
    return a + b
}

console.log(`Soma Arrow Function = ${arrowSum(2, 8)}`)

const arrowSub = (a, b) => a - b

console.log(`Subtração Arrow Function = ${arrowSub(8, 3)}`)

